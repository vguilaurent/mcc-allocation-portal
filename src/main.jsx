import React from 'react'
import ReactDOM from 'react-dom/client'
import AllocationPortal from './portal'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AllocationPortal />
  </React.StrictMode>,
)
